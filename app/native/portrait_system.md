# Portrait system

Portraits are keyed by stable NPC identity, age band, gender presentation, origin, faction, scars, social role and current state. The client must load one portrait only when a character is shown; never preload the entire portrait library. AI-generated artwork is stored as local assets for production builds, while metadata remains text-based and deterministic.
